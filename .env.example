# Скопируйте в .env и поменяйте значения перед любым непубличным/публичным запуском.
# ОБЯЗАТЕЛЬНО смените секреты перед продом.

JWT_SECRET=change-me-in-prod
ADMIN_EMAIL=admin@jailtracker.kz
ADMIN_PASSWORD=admin123

# Белый список origin'ов фронта через запятую (CORS НЕ "*").
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
