# Скопируйте в .env (frontend/.env) — Vite подхватит переменные с префиксом VITE_.
# Без ключа карта откатится на растровый OpenStreetMap.

# Ключ MapTiler. ВАЖНО: в кабинете MapTiler ограничьте ключ по Allowed origins
# (домен фронта), иначе лимит можно сжечь с чужого сайта.
VITE_MAPTILER_KEY=qGy3UlFcO8PDDDOsQMqU
